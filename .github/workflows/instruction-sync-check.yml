name: Instruction Sync Check

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  check-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify AGENTS mirror sync
        run: bash scripts/check-agent-instructions-sync.sh