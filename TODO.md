# TODO

- [ ] Add a CI/check script that fails when `AGENTS.md` and `.github/copilot-instructions.md` are out of sync.
- [ ] Harden video-analysis output: avoid medical/identity speculation, require confidence-aware observations, and use explicit unknowns when uncertain.
- [ ] Evaluate creating a dedicated agent workspace (separate from source-code workspace) and define boundaries, mounts, and data-sharing contracts.
- [ ] Expand baseline agent guidance files for assistant behavior (help-first UX, tone, safety rails, escalation paths), beyond coding-agent defaults.
